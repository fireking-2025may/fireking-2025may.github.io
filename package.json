{
  "name": "ubta-s77-letter-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Offline UBTA S77 letter case-entry application",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests scripts",
    "test": "npm run build && node --test tests/unit/*.test.js tests/integration/*.test.js",
    "test:browser": "playwright test tests/browser",
    "check": "npm run lint && npm run typecheck && npm test"
  },
  "dependencies": {
    "decimal.js": "10.6.0"
  },
  "devDependencies": {
    "@eslint/js": "9.32.0",
    "@playwright/test": "1.54.2",
    "esbuild": "0.25.8",
    "eslint": "9.32.0",
    "typescript": "5.9.2"
  },
  "license": "MIT"
}
